import LoginView from '../features/auth/LoginView';

export const metadata = {
  title: 'Login | Wenlance',
  description: 'Sign in to your Wenlance account',
};

export default function LoginPage() {
  return <LoginView />;
}
